<div class="section" id="section">
  <div class="layout">
    {{> sidebar}}
    <div class="page" id="scrollContent">
      <div class="title">
        <div class="title-name-track-by-artist">
          Синглы и EP <a class="href-decoration" href="artist/{{ artistId }}">{{ artistName }}</a>
        </div>
      </div>
      <div class="grid-layout">
        {{#each singles}}
        <a href="/album/{{ id }}" class="card">
          <img src="{{ cover }}" alt="{{ name }}" />
          <div class="card-bottom-info">
            <p class="card-name">{{ name }}</p>
            <p class="card-sub">
              {{ type }}
              <span class="dot"></span>
              {{ year }}
            </p>
          </div>
          <!--<button class="play-button play-button-placement"></button>-->
        </a>
        {{/each}}
      </div>
    </div>
  </div>
</div>
