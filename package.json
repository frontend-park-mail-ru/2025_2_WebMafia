{
  "name": "webmafia",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./compilehbs.sh && node server.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,hbs,css,json,md}\"",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:css:fix": "stylelint \"**/*.css\" --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "handlebars": "^4.7.8"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
